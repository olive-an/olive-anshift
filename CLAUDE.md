# おりーぶ庵 プロジェクトルール

## 【最重要】元フォーマット完全維持（全様式共通・恒久ルール）

Word / Excel / その他の指定フォーマットに記入する際は、**新規作成せず、必ず元ファイルを直接編集**する。
「元ファイルをテンプレートとして保持し、必要箇所だけを書き換える」方法で処理すること。

### 変更禁止事項
- 表の罫線を削除・変更しない／表を作り直さない
- セルの結合・分割を変更しない
- 行の高さ・列の幅・セルの位置を変更しない
- フォント・文字サイズ・文字色を変更しない
- 行間・段落前後の間隔・インデントを変更しない
- 余白・改ページ位置・ヘッダー/フッター・ページ設定を変更しない
- 既存の項目名・見出しを変更しない／既存の文章を勝手に削除しない

### 入力方法
- 指定された入力欄・セルの中にのみ入力する
- 文章量が多くても、表・罫線・セル・レイアウトを変更して調整することは禁止
- 入力文章の書式は、その入力欄に元から設定されている書式を引き継ぐ

### 完成前チェック（毎回実施し、結果を報告する）
1. 罫線がすべて残っている
2. 表の位置が変わっていない
3. 行間が変わっていない
4. 列幅・行高が変わっていない
5. ページ数・改ページが不必要に変化していない
6. フォント・文字サイズが変わっていない
7. 入力対象以外の箇所が変更されていない

書式を維持できない箇所がある場合は、勝手に変更せず、その箇所を報告する。

### 実装上の指針（ai-os.html で確立済みの方式）
- **Excel(.xlsx)**: SheetJSの書き出しはセル書式を破壊するため使用禁止。
  JSZipで worksheet XML の該当セル値のみ inlineStr で書き換える（styles.xml・結合・列幅・用紙設定に触れない）。
  検証は openpyxl で罫線セル数・結合数を元ファイルと比較する。
- **Word(.docx)**: JSZipで document.xml 等のトークン/対象テキストのみ置換。文書構造・スタイルには触れない。
- 元ファイルは変更せず、記入結果は新しいファイルとして保存する。

## その他
- `index.html` はシフト希望フォームの本番。AI-OS開発では変更しない。
- `ai-os.html` はNetlify `olive-ai-os` へGitHub Actionsで自動デプロイされる
  （https://olive-ai-os.netlify.app/ai-os.html）。
- 利用者・職員の実個人情報を、公開されるファイル（ai-os.html等）やサンプルに埋め込まない。
