# おりーぶ庵 プロジェクトルール

## 【最重要】元フォーマット完全維持（Word・Excel・全様式共通の恒久ルール）

指定フォーマット（Word/Excel等）に記入する際は、**新規ファイルを一から作成せず、必ず元ファイルを直接編集**する。
「元ファイルをテンプレートとして保持し、指定された入力箇所・セルにのみデータを入力する」方法で処理すること。

## Excel：絶対に変更しない項目

- セルの罫線／罫線の種類・太さ
- セルの背景色／文字色／フォント／文字サイズ／太字などの文字装飾
- セルの結合・結合解除
- 行の高さ／列の幅
- 文字の配置（中央揃え・右揃え・左揃え／上下の配置）
- 折り返して全体を表示する設定／縮小して全体を表示する設定
- 表示形式（日付形式・数値形式・パーセント表示）
- 既存の数式／関数／セル参照
- シート名／シートの順番／非表示の行・列・シート
- 入力規則／条件付き書式／名前付き範囲／フィルター設定
- 印刷範囲／改ページ／ページ設定／用紙サイズ／印刷方向／余白
- ヘッダー・フッター／拡大縮小印刷の設定

## Word：絶対に変更しない項目

- 表の罫線／表の作り直し禁止
- セルの結合・分割／行の高さ・列の幅／セルの位置
- フォント・文字サイズ・文字色
- 行間／段落前後の間隔／インデント
- 余白／改ページ位置／ヘッダー・フッター／ページ設定
- 既存の項目名・見出し／既存の文章の削除禁止

## 入力ルール

- 指定されたセル・入力欄にのみ値を入力する
- 既存セル・入力欄に設定されている書式をそのまま引き継ぐ
- 文章が長くても、列幅・行高・表・レイアウトを勝手に変更しない
- セルに入りきらなくても、フォントサイズを勝手に小さくしない
- 入力場所が分からない場合は、推測して入力せず確認する

## 数式・関数の保護

- 数式・関数が設定されているセルには、指示がない限り値を直接入力しない
- 数式を値に変換することは禁止
- 数式の参照先も変更しない

## 行政様式・指定様式の場合

- 様式そのものの変更は禁止
- 入力欄への記入だけを行い、行・列・セル・シートの追加や削除はしない

## 保存

- 編集後はExcel形式（.xlsxまたは元ファイルと同じ形式）を維持する
- CSVへの変換は禁止
- 元ファイルは上書きせず、原本を残した状態で別名保存する

## 完成前チェック（毎回実施し、結果を報告する）

### Excel（10項目）
1. 罫線が消えていない
2. 行高・列幅が変わっていない
3. セル結合が変わっていない
4. フォント・文字サイズが変わっていない
5. 数式・関数が消えていない
6. シート構成が変わっていない
7. 印刷範囲・改ページが変わっていない
8. 入力対象以外のセルが変更されていない
9. 行政指定様式のレイアウトが維持されている
10. 元ファイルと同じ形式で保存されている

### Word（7項目）
1. 罫線がすべて残っている
2. 表の位置が変わっていない
3. 行間が変わっていない
4. 列幅・行高が変わっていない
5. ページ数・改ページが不必要に変化していない
6. フォント・文字サイズが変わっていない
7. 入力対象以外の箇所が変更されていない

書式を維持したまま入力できない箇所がある場合は、勝手に修正せず、どのセル・箇所に問題があるか報告する。

## 実装上の指針（ai-os.html で確立済みの方式）

- **Excel(.xlsx)**: SheetJS（XLSX.write）での書き出しはセル書式を破壊するため**使用禁止**。
  JSZipで worksheet XML の該当セル値のみ inlineStr で書き換える
  （styles.xml・sharedStrings・結合・列幅・数式・用紙設定・他シートに一切触れない）。
  検証は openpyxl 等で罫線セル数・結合数・数式を元ファイルと比較する。
- **Word(.docx)**: JSZipで document.xml 等の対象テキストのみ置換。文書構造・スタイルには触れない。
- SheetJSは**読み取り（インポート）専用**として使用可。

## その他

- `index.html` はシフト希望フォームの本番。AI-OS開発では変更しない。
- `ai-os.html` はNetlify `olive-ai-os` へGitHub Actionsで自動デプロイされる
  （https://olive-ai-os.netlify.app/ai-os.html）。
- 利用者・職員の実個人情報を、公開されるファイル（ai-os.html等）やサンプルに埋め込まない。
